name: innovation-challenge-chat
metadata:
  template: innovation-challenge-chat@1.0.0

infra:
  provider: bicep
  path: infra

services:
  web:
    project: .
    language: js
    host: containerapp
    docker:
      path: Dockerfile
      context: .
